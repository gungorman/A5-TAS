from pca import pca
import csv





"""
data = open("EHAM_LIMC.csv", newline="")
X = data.data
Y = data.target
labels = data.feature_names

model = pca(normalize=True)

results = model.fit_transform(X, col_labels=labels, row_labels=Y)
"""
